//! El contrato REST de AIDA vive en `examples/aida/src/rest.zig`.
//! El encoding de `Fecha` (objeto JSON ↔ texto DATE) está en `fecha_wire.zig`.
//! El módulo de build `aida_rest` apunta a `examples/aida/src/rest.zig`.
